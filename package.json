{
  "name": "electron-typescript-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "yarn run build && electron ./dist/main.js"
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^7.1.6",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
